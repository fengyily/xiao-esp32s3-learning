# 复制为 .env 并填入真实值（.env 已被 gitignore，不会上传）
# 用法：source .env 后再 go run .  （或用 direnv / 你习惯的方式）

export ALIYUN_AK_ID=你的AccessKeyID
export ALIYUN_AK_SECRET=你的AccessKeySecret
export ALIYUN_APPKEY=你的智能语音项目Appkey

# 可选，默认值如下
export ALIYUN_REGION=cn-shanghai
export LISTEN_ADDR=:8080
