# XIAO ESP32-S3 对话功能规划

> 终极目标：对着板子说话，它能听懂并语音回应你 —— 一个口袋里的 AI 语音助手。
> 路线已定：**本地唤醒 + Go 中转服务 + 云端**（板子只管录音/放音，复杂活全交给 Go 服务）。

---

## 已确定的关键决策（2026-06-11）

| 决策点 | 选择 | 理由 |
|---|---|---|
| 算力路线 | 本地唤醒 + 云端对话 | 板子跑不动 ASR/LLM |
| 语音识别 ASR | **阿里云一句话识别** | 国内访问稳、中文好 |
| 鉴权/签名 | **用 Go 中转服务代理** | 阿里云 POP 签名在 ESP32 手写太难；密钥留服务器更安全 |
| 对话大脑 | Claude API（放在 Go 服务里，后续接） | 你熟 |
| 语音输出 | 先**串口打印回答**，喇叭(MAX98357A+喇叭)待购后再放音 | 喇叭未到货 |
| 第一版范围 | **Go 只做 ASR**：板子录音→Go→阿里云→返回文字 | 分步调试，先验证录音+识别链路 |
| 服务部署 | 地址做成配置(secrets.h)，跑时再填 | 灵活 |

---

## 一、整体架构（Go 中转版）

```
 [板子 ESP32]                 [你的 Go 中转服务]               [云]
  录音 PCM ──POST /asr───────▶ 调阿里云 ASR(签名/Token) ──────▶ 阿里云
       ◀──── {text:"..."} ───── 返回识别文字
  串口打印识别文字
       ⋮ (后续)                 (后续)文字发 Claude ──────────▶ Claude API
       ⋮ (后续)                 (后续)TTS 合成语音
  (喇叭到货后)播放回答
```

**核心判断**：板子算力跑不动 ASR/LLM，且阿里云签名在 ESP32 难写。
所以加一层 **Go 中转服务**：板子只 POST 音频、收文字；阿里云签名、密钥、Claude、TTS 全在 Go 端。
板子端代码因此非常简单：录音 → POST → 收文字 → 打印。

---

## 二、能力盘点：我已经有什么，还缺什么

| 环节 | 需要的能力 | 现状 | 来自 |
|---|---|---|---|
| 唤醒 | 麦克风阈值检测 | ✅ 有 | Demo 5 + Demo 9(待做) |
| 录音 | I2S 采集 → WAV | ✅ 有采集，差 WAV 封装 | Demo 5 |
| 联网上传 | WiFi + HTTP POST | ✅ 有 WiFi，差 HTTPClient | Demo 7/8 |
| 语音识别 ASR | 云端 API | ⬜ 待接入 | 云 |
| 对话大脑 | Claude API | ⬜ 待接入 | 云 |
| 语音合成 TTS | 云端 API | ⬜ 待接入 | 云 |
| 下载播放 | I2S 输出 + **喇叭** | ❌ **缺硬件** | 需外购 |

**唯一的硬件缺口：喇叭。** 其它要么已具备，要么是写代码就能补上的软件能力。

---

## 三、必须购买的硬件

| 硬件 | 作用 | 说明 |
|---|---|---|
| **MAX98357A I2S 功放模块** | 把数字音频转成喇叭能放的信号 | ESP32 做语音输出的标准方案，几十块 |
| **小喇叭（4Ω/8Ω, 1~3W）** | 出声 | 配合 MAX98357A |
| （可选）杜邦线/面包板 | 接线 | MAX98357A 接 I2S：BCLK/LRC/DIN + 电源地 |

> ESP32-S3 本身能驱动 I2S 音频输出，只是这块板没带喇叭，所以要外接功放+喇叭。
> 麦克风（输入）板载已有，不用买。

---

## 四、分阶段实现路线（建议按顺序，每步都能独立验证）

### 阶段 A：把"录音"做扎实（不需要喇叭，现在就能做）
- **A1. Demo 9 声控触发** — 拍手/喊一声触发动作，练"什么时候开始录音"（唤醒雏形）。
- **A2. 录音存 WAV** — 用 I2S 采集 2~3 秒（Demo 5 的采集 + WAV 文件头封装），存到 SD 卡（Demo 6 的 SPI 存卡），拔卡在电脑上能播放 = 录音链路通。
  - 复用：Demo 5(采集) + Demo 6(SD SPI 存卡) + Demo 9(触发)

### 阶段 B：把"上传 + 云端识别"打通（不需要喇叭）

- **B0. Go 中转服务 (ASR)** — ✅ **已完成**（src/server）。`POST /asr` 收 PCM → 阿里云一句话识别 → 返回 `{text}`。
  - 已验证：用 mac `say` 生成中文 PCM 测试，识别出"你好，今天天气怎么样？"，0.5s，带标点。
  - POP 签名算法经阿里云真实校验通过；密钥走 .env(gitignore)。
- **B1. 板子上传音频到 Go 服务** — ✅ **已完成**（Demo 11）。板子录音 PCM → `HTTPClient` POST → 解析串口打印。
  - 复用：Demo 5(麦克风) + Demo 7(WiFi) + PSRAM；触发改用**声控**（按钮 D1/GPIO2 已损坏）。
- **B2. 接 DeepSeek 对话** — ✅ **已完成**（Demo 11 + Go `/chat`）。识别文字发 DeepSeek，返回回答串口打印。
  - 大脑改用 **DeepSeek**（不是 Claude）：OpenAI 兼容、国内快、中文好。真机验证可对话。
- **B3. 实时中英互译** — ✅ **已完成**（Demo 12 + Go `/translate`）。
  - 说中文出英文、说英文出中文（Go 端按"识别结果有无中文字符"自动判方向，DeepSeek 翻译）。
  - 真机验证："流血的仕途"→"The Bloody Officialdom"。后续接喇叭即可语音播报译文。

#### 阶段 B 期间踩的几个大坑（都已解决）

- **按钮脚 GPIO2 物理损坏**：反复金属短接 D1↔GND 把 ADC/Touch 复用脚弄坏（数字/ADC/触摸三种读法实测全恒0）。改为**声控触发**，不再碰引脚。
- **⭐ PDM 录音又闷又糊（最难的坑）**：老 Arduino `I2S` 库 PDM 实现有缺陷——① 实际采样率≈设定值一半（设16k实测8339、设32k实测17469）；② 高频被砍到 2kHz 以下（频谱实测 >2kHz 几乎为零）。根因：库没正确配 PDM 下采样、`fixed_mclk=512×` 对 PDM RX 不对。**解决：改用原生 `driver/i2s.h`，显式 `i2s_set_pdm_rx_down_sample(DSR_8S)`**，录音清晰、采样率正确 16k。
- **声控的工程细节**：预缓冲（留住触发那个字不丢）、峰峰值 VAD（区分语音 vs 噪声/直流偏置——噪声不过零、语音过零）、HTTP 重试（治偶发 -1）、识别空不调 LLM。

### 阶段 C：把"语音回应"补上（需要喇叭到货 MAX98357A）

- **C1. I2S 播放测试** — 喇叭接好，先放一段 WAV 确认能出声。
- **C2. 接 TTS** — 回答/译文发语音合成 API，拿回音频播放。

### 阶段 D：串成完整闭环

- 录音 → 识别 → DeepSeek(对话/翻译) → TTS → 播放，一气呵成 + 体验优化。

---

## 五、云端 API 选型（待确认）

| 环节 | 默认建议 | 备选 |
|---|---|---|
| 对话大脑 | **Claude API**（你熟，效果好） | — |
| 语音识别 ASR | 国内：阿里/讯飞/百度；或 OpenAI Whisper | 看延迟和成本权衡 |
| 语音合成 TTS | 国内：阿里/讯飞；或云端 TTS | 选音色自然的 |

> API Key 同样走 `secrets.h`（已在 .gitignore 里忽略），不进仓库。
> 待你确认用哪家 ASR/TTS 后，B1/C2 的具体接口代码再细化。

---

## 六、已知难点与风险（提前知道，到时不慌）

1. **WAV 封装** — I2S 出来的是裸 PCM，要手动加 44 字节 WAV 头，ASR 才认。不难，有现成格式。
2. **音频上传体积** — 几秒录音几百 KB，注意分块上传 / 内存（用 PSRAM）。
3. **HTTPS 证书** — 调云端 API 多为 HTTPS，`WiFiClientSecure` 可能要处理证书或设 `setInsecure()`。
4. **延迟** — 录音→上传→三段云端→下载→播放，整链路可能几秒，体验上要有"思考中"提示（LED/提示音）。
5. **MAX98357A 接线** — I2S 三线 BCLK/LRC/DIN 别接错；注意和板载麦克风的 I2S 是否复用引脚（输入输出可能要分开的 I2S 通道）。
6. **同时用麦克风(输入)和喇叭(输出)** — 录音和放音是两个方向的 I2S，要么分时复用、要么用不同 I2S 端口，到时验证。

---

## 六-B、阿里云一句话识别接口规范（研究所得，写 Go 服务用）

### 识别接口（简单，Go 直接调）

- 方法/URL：`POST http(s)://nls-gateway-<region>.aliyuncs.com/stream/v1/asr`
  （region 如 `cn-shanghai`；海外 `ap-southeast-1`）
- 请求头：
  - `X-NLS-Token: <token>`（必选）
  - `Content-Type: application/octet-stream`
  - `Content-Length: <音频字节数>`
- URL 查询参数：`appkey=<你的项目Appkey>`、`format=pcm`、`sample_rate=16000`、
  可选 `enable_punctuation_prediction=true`（加标点）
- 请求体：**原始 PCM 音频字节**，16000Hz / 16bit / 单声道，≤60 秒
  → **正好是 Demo 5 麦克风的格式**，板子录完直接传
- 返回 JSON：`{ "task_id":"…", "result":"识别文字", "status":20000000, "message":"SUCCESS" }`
  取 `result` 即识别结果

### Token（难点，放 Go 服务做）

- CreateToken 是阿里云 POP 协议签名：参数按字典序排，HMAC-SHA1，
  key = `AccessKeySecret + "&"`，结果 base64 再 URL 编码。
- Token 有几小时有效期（响应含 ExpireTime），可缓存复用，不用每次签。
- **Go 有官方 SDK / 现成示例**，比 ESP32 手写省事得多 —— 这正是用 Go 中转的主因。

> 文档来源：阿里云智能语音交互 RESTful API（help.aliyun.com/zh/isi）。

---

## 七、下一步

- **现在能立刻做的**：阶段 A（Demo 9 + 录音存 WAV），完全不依赖喇叭。
- **需要等硬件**：阶段 C 之后（买 MAX98357A + 喇叭）。
- **需要你定的**：ASR / TTS 用哪家（对话大脑默认 Claude）。

> 进度同步到 LEARNING_PLAN.md；这份文档随实现推进持续更新。
